# v1: anti-pattern baseline
# - base image ใหญ่มาก (python:3.12 full Debian)
# - pip cache ไม่ถูกลบ → ฟองในทุก layer
# - COPY . . ก่อน install → cache miss ทุกครั้ง
# - รันเป็น root
FROM python:3.12

WORKDIR /app
COPY app/ .
RUN pip install -r requirements.txt

CMD ["python", "app.py"]
